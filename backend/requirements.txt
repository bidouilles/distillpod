fastapi>=0.110.0
uvicorn[standard]>=0.27.0
aiosqlite>=0.20.0
feedparser>=6.0.11
# Transcription engines are optional — pick one via STT_BACKEND.
#   voxtral: nothing to install, just MISTRAL_API_KEY
#   mlx:     pip install mlx-whisper   (macOS arm64, uses the GPU)
#   whisper: faster-whisper below      (CPU everywhere)
faster-whisper>=1.0.0
httpx>=0.27.0
requests>=2.31.0
markdown>=3.5

python-dotenv>=1.0.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9

# Auth
authlib>=1.3.0
python-jose[cryptography]>=3.3.0
itsdangerous>=2.1.0
